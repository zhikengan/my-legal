version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Node version:"
        - node -v
        - npm -v
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
